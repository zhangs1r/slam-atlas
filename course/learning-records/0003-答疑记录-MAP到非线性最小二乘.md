# 答疑记录 0003：从 MAP 到非线性最小二乘的四个公式

> 类型：**通道 B（独立答疑页）+ 通道 A（就地插入补丁）** 双落点、双向互链
> 产物 ①：`qa/qa-0002-MAP到非线性最小二乘.html`（独立答疑页，**不占主线课号**）
> 产物 ②：`lessons/0002-PastPresentFuture-2016-SLAM的全景纲领.html` 内 `id="q0002-map"` 就地补丁
> 日期：2026-09-17

## 学习者的问题

> "0002 课「从 MAP 到非线性最小二乘」中的四个公式我不是很理解，你可以尝试画图或者帮我回顾一下数学知识，
> 因为我很久没接触数学推导了，一直在实践项目。"

## 结构要求（学习者在第二轮追问中明确提出，已固化为项目约定）

> "你可以单独建一个文件夹，专门里面就放答疑，可以不用给他插入到这种零零几零几这种课程里面了，
> 你发个链接从那个正文跳转到我这个答疑里面，我这答疑里面可以有个链接，再跳回到相应的位置。"

据此（见 `course/NOTES.md` 第三节、skill 第 9.2 节）：

- 答疑页放独立目录 **`course/qa/`**，命名 **`qa-<源课号>-<主题>.html`**；
- **不占用主线课号**——主线永远是 0001、0002、0003…（第二季仍从 0017 开始，
  不会因为一页答疑而顺延，也避免与并行会话写的第二季撞号）；
- **双向互链**：源课 `(4)` 式之后插补丁（带 `id="q0002-map"`）→ 链接跳到答疑页；
  答疑页 hero 里给「← 回到 0002 课原位置」链接（`../lessons/0002-…#q0002-map`），页脚再给一条；
- catalog 里单独建「追问答疑馆」分馆（`qa` → collection `qa-0002`），**不挂进 `stage-a`**，
  `lesson.id` 用 `QA0002`，避免与主线课号混淆。

## 判断：为什么走"通道 B + A"而不是只插一个补丁

| 判据（skill 9.0） | 本例 |
|---|---|
| 需要**独立推导** | ✅ 贝叶斯分母可扔、独立 → 连乘、高斯 → exp(加权平方)、取负对数翻面，共 4 段推导 |
| **跨知识点**（概率 + 概率分布 + 优化 + 数值稳定性） | ✅ 涉及贝叶斯、高斯、二次型范数、argmax/argmin、log 数值下溢、迭代求解 |
| 学习者**自陈基础薄弱**（很久没碰数学） | ✅ 需要"补零件"式的从零铺垫，塞进原课会撑爆原课节奏 |

结论：**主体必须独立成页**（`qa/qa-0002-*`），同时在 0002 被问到的位置留一个默认展开的补丁，
把"一句话结论 + 四行各自只做一件事 + 跳转链接"就地给出。

## 答疑页的结构（8 节）

| 节 | 内容 | 对应原始疑问 |
|---|---|---|
| 1 | **先补五个数学零件**：p(·) 读成"可信度"、贝叶斯（分母可扔）、高斯与 Ω、argmax/argmin、log 与取负 | "帮我回顾数学知识" |
| 2 | 第 (1) 式：等号来自贝叶斯 + 分母可扔；三条候选取哪个的**数值算例**（0.80×0.20 等） | 第 (1) 式 |
| 3 | 第 (2) 式：**把两个假设拆开**（量测独立 vs 局部依赖），因子图举例，log 的工程理由（下溢表） | 第 (2) 式 |
| 4 | 第 (3) 式：从一维高斯一步步推到 $\exp(-\frac12\|h-z\|^2_\Omega)$，解释 ∝ 与二次型 | 第 (3) 式 |
| 5 | 第 (4) 式：四步化简链（乘积→求和→脱 exp→取负翻面）；**先验项被原文省略**的诚实说明 | 第 (4) 式 |
| 6 | **手算到底**：走廊里一台机器人，里程计 10 m(σ=1) + 激光 14 m(σ=0.5) → $x^\star=13.2$ m，含"拉力平衡"验算与先验情形 | "推不动" → 用算术代替推导 |
| 7 | **七个最容易卡住的点**（先验为何能扔、两个假设之别、为何取 log、Ω 为何用逆、高斯不成立怎么办、非线性怎么解、与卡尔曼的关系） | 隐含疑问 |
| 8 | 自测表 + 2 个互动判断 + 6 道折叠练习 | 检索练习 |

## 设计取舍（下次可复用）

1. **"补零件 → 逐式拆解 → 手算 → FAQ → 自测"** 是应对"我很久没碰数学"类追问的固定骨架。
   零件不进正文而是集中放在第 1 节，学习者可以按需回查，不必在公式中间被术语打断。
2. **每一节都配一张自绘图**（共 7 张，纯内联 SVG，不引外部库）：
   - 图 1 候选轨迹打分图（房间 + 三条轨迹 + 分数条）
   - 图 2 三个同学量同一张桌子（解释"量测独立"）
   - 图 3 论文 Fig. 3 的简化因子图（保留原图蓝/绿配色约定）
   - 图 4 高斯钟形曲线 + Ω 的胖瘦对照（两条曲线为**真实计算的高斯折线**，
     $e=1$ m 处标注 13.5% = $\exp(-2)$，可核对）
   - 图 5 山峰 ↔ 山谷（max 翻 min，峰顶与谷底同横坐标）
   - 图 6 弹簧平衡图（细长弹簧 vs 粗短弹簧 + 两根等长"拉力"色条）
   - 图 7 三种权重下估计值的滑动对照（13.2 / 12.0 / 10.8）
   所有图的**解释文字都放在 `<figcaption>` 里**（图内只留最短标签），并统一给
   「这张图在画什么 / 怎么看这张图 / 要记住的一句话」三段。
   **曲线上的数值点用 Python 按公式算好再写进 `<polyline>`**（脚本见 `_tmp/gen_poly.py`），
   不凭手感画。
3. **数值算例必须能验算**。13.2 m 这个数满足 $\sum\omega_k(x-z_k)=0$（两边拉力都是 3.2），
   学习者可以用纸笔复核；钟形曲线的 13.5% 也对应 $\exp(-2)$。**不要给无法复核的"看起来很像"的数。**
4. **诚实标注"教学用假数字"**：第 2 节那张候选取值表（0.80 / 0.10 / 0.40 等）是我们为演示乘法编的，
   不是论文数值，课里已显式写明。
5. **SVG 里不写 `class` 属性**（只写内联 `fill`/`stroke`），既避免与课程样式表打架，
   也避免第 11 节第 ⑥ 项的"未定义类"误报。

## 顺手核出的两处原文细节（重要）

1. **原文 (4) 式省略了先验项。** 原文第 II-A 节在 (4) 式前一段写了
   "assume that the prior can be written as $p(\mathcal{X})\propto\exp(-\frac12\|h_0(\mathcal{X})-z_0\|^2_{\Omega_0})$"，
   (4) 式第一行也确实是 $\operatorname{argmin}-\log(p(\mathcal{X})\prod_k p(z_k\mid\mathcal{X}_k))$，
   **但最终那行求和只从 $k=1$ 到 $m$，没有显式写出先验项**。
   这不是错误、是排版省略，但如果不点明，学习者会觉得"第 0002 课说先验可丢，怎么先验就再也不见了"。
   已在答疑页第 5 节用 `callout` 给出完整形式。
2. **OCR 错误 4 处**（已在答疑页第 7 节末尾集中说明）：
   argmax 下标 `χ` → 应为花体 $\mathcal{X}$；(2) 式多一个右括号；
   `squared <sub>2</sub>-norm` → 应为 squared ℓ₂-norm（ℓ 被吃）；`ex p ( - ...)` → `exp` 被断字。

## 后续动作

- [x] 新增 `qa/qa-0002-MAP到非线性最小二乘.html`（独立目录，不占课号）
- [x] 在 0002 第 3 节 (4) 式后插入 `qa-patch`（`id=q0002-map`，默认 `open`，含跳转链接）
- [x] 答疑页 hero 加「回到原位置」提示条，页脚加回程链接（指向 `../lessons/0002-…#q0002-map`）
- [x] `data/course-catalog.js`：新增分馆 `qa`（追问答疑馆）+ collection `qa-0002`；
      `lessons` 追加 `QA0002`（`paper: "qa-0002"`）；**`stage-a` 的课时数已复原为 16 节课**
- [x] `learning-records` 追加本条；`reference/glossary.html` 的 MAP/MLE/因子图/信息矩阵 课次标注
      改为「0002 ＋答疑页」，并新增「加权最小二乘」一条
- [x] 已跑 skill 第 11 节代码校验（含新增的第 ⑨ 项双向链接校验）+ 第 12 节视觉校验

## 方法论收获

**"我看不懂这几行公式"这类追问，本质不是"公式难"，而是"零件缺"。**
如果只把公式再讲一遍（换个说法重抄一遍），学习者下次还是卡；
正确做法是**先列出他缺的那几个零件**（本例 5 个），再让公式变成"零件的组装说明书"。
判断零件清单的方法：逐式追问"这一步用了什么性质"——用到的每个性质就是一个零件。
另配两条：
① **能验算的数值算例比三页推导更能让人产生"我懂了"的感觉**，所以这类答疑页必须有一节"手算到底"；
② **答疑页不要混进课程主线**——它是"哪里不懂点哪里"的旁支，
   一旦混进编号序列，主线进度、课时统计、后续开课都会被牵连（本次即为真实教训）。
